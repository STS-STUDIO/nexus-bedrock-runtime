# Nexus local patch — uncommitted working-tree diff of submodule 'game-window'
# Applies on pinned commit: d073c2aace1521ae32d239ca4fc26b97e4b4a27e
# Captured 2026-07-12 from the exact tree that built Nexus Bedrock Runtime v1.7.6-572-nexus5.
# Patch 0003: OpenGL ES 3.1 context for MC 1.26+ (window_glfw.cpp).
# Apply with: git -C <manifest>/game-window apply <this file>
#
diff --git a/src/window_glfw.cpp b/src/window_glfw.cpp
index 7e0a85b..523bb72 100644
--- a/src/window_glfw.cpp
+++ b/src/window_glfw.cpp
@@ -18,7 +18,7 @@ GLFWGameWindow::GLFWGameWindow(const std::string& title, int width, int height,
         glfwWindowHint(GLFW_CLIENT_API, GLFW_OPENGL_ES_API);
         glfwWindowHint(GLFW_CONTEXT_CREATION_API, GLFW_EGL_CONTEXT_API);
         glfwWindowHint(GLFW_CONTEXT_VERSION_MAJOR, 3);
-        glfwWindowHint(GLFW_CONTEXT_VERSION_MINOR, 0);
+        glfwWindowHint(GLFW_CONTEXT_VERSION_MINOR, 1);   // Nexus patch 0003: MC 1.26+ needs OpenGL ES 3.1
     } else if(api == GraphicsApi::OPENGL) {
         glfwWindowHint(GLFW_CONTEXT_VERSION_MAJOR, 3);
         glfwWindowHint(GLFW_CONTEXT_VERSION_MINOR, 2);
