diff --git a/CMakeLists.txt b/CMakeLists.txt
index 5c50f36..621502e 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -85,6 +85,8 @@ if(USE_IMGUI AND IS_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}/../imgui" AND EXISTS
         src/glad.c
         src/imgui_ui.cpp
         src/imgui_ui.h
+        src/nexus_mods.cpp
+        src/nexus_mods.h
     )
 
     target_include_directories(mcpelauncher-client BEFORE PRIVATE
diff --git a/src/imgui_ui.cpp b/src/imgui_ui.cpp
index 3bf28d5..3322f17 100644
--- a/src/imgui_ui.cpp
+++ b/src/imgui_ui.cpp
@@ -1,5 +1,7 @@
 #include "imgui_ui.h"
+#include <cstdlib>
 #include "settings.h"
+#include "nexus_mods.h"
 
 #include <time.h>
 #include <game_window_manager.h>
@@ -196,6 +198,10 @@ static void ReloadFont() {
     fontVeryLargeSize = io.Fonts->AddFontFromMemoryTTF(data, data_size, (int)ceil(36 * Settings::scale), &fontConfig);
 
     IM_FREE(data);
+
+    // Hand the freshly built faces to the Nexus modules. ReloadFont runs again
+    // whenever the UI scale changes, so this has to be re-published each time.
+    NexusMods::setFonts(NexusMods::Fonts{fontDefaultSize, fontMediumSize, fontLargeSize, fontVeryLargeSize});
 }
 
 struct MenuEntry {
@@ -588,10 +594,12 @@ void ImGuiUIDrawFrame(GameWindow* window) {
         }
     }
     
-    // Update shared state with current ImGui wants
+    // Update shared state with current ImGui wants. The Nexus mod menu takes
+    // input for itself the moment it opens, without waiting a frame for ImGui's
+    // own hover bookkeeping to catch up.
     sharedInputState.wantTextInput = io.WantTextInput;
-    sharedInputState.wantCaptureMouse = io.WantCaptureMouse;
-    sharedInputState.wantCaptureKeyboard = io.WantCaptureKeyboard;
+    sharedInputState.wantCaptureMouse = io.WantCaptureMouse || NexusMods::isMenuOpen();
+    sharedInputState.wantCaptureKeyboard = io.WantCaptureKeyboard || NexusMods::isMenuOpen();
     static auto showMenuBar = true;
     static auto menuFocused = false;
     auto now = std::chrono::high_resolution_clock::now();
@@ -603,7 +611,14 @@ void ImGuiUIDrawFrame(GameWindow* window) {
         auto secs = std::chrono::duration_cast<std::chrono::milliseconds>(now - mouseOnY0Since).count();
         showMenuBarViaMouse = secs >= 500;
     }
-    auto autoShowMenubar = (!window->getFullscreen() || showMenuBarViaMouse || menuFocused);
+    // Nexus: Right Shift (or whatever nexus_menu_key names) opens the mod menu,
+    // Java-client style. The upstream menubar stays hidden unless the mouse rests
+    // at the top edge (500ms) or it has keyboard focus, so the game looks clean.
+    NexusMods::handleMenuHotkey();
+    if(NexusMods::isMenuOpen()) {
+        ImGui::SetNextFrameWantCaptureKeyboard(true);
+    }
+    auto autoShowMenubar = (showMenuBarViaMouse || menuFocused);
     static auto showFilePicker = false;
     static auto show_demo_window = false;
     static auto show_confirm_popup = false;
@@ -754,6 +769,8 @@ void ImGuiUIDrawFrame(GameWindow* window) {
             }
             ImGui::EndMenu();
         }
+        // Nexus: every module, one click each, plus a way into the full mod menu.
+        NexusMods::drawMenubarSection(window);
         if(ImGui::BeginMenu("Help")) {
             ImGui::MenuItem("About", nullptr, &show_about);
             ImGui::EndMenu();
@@ -766,6 +783,11 @@ void ImGuiUIDrawFrame(GameWindow* window) {
         menuFocused = false;
         lastwantfocusnextframe = false;
     }
+
+    // Nexus mod menu. Owns its own window and HUD layout; see nexus_mods.cpp.
+    NexusMods::drawMenu(window);
+    NexusMods::drawHud(window);
+
     // Always center this window when appearing
     ImVec2 center = ImGui::GetMainViewport()->GetCenter();
     ImGui::SetNextWindowPos(center, ImGuiCond_Appearing, ImVec2(0.5f, 0.5f));
diff --git a/src/jni/lib_http_client.cpp b/src/jni/lib_http_client.cpp
index 3017fb6..e60b537 100644
--- a/src/jni/lib_http_client.cpp
+++ b/src/jni/lib_http_client.cpp
@@ -3,9 +3,40 @@
 #include <log.h>
 #include <curl/curl.h>
 #include <thread>
+#include <functional>
+#include <pthread.h>
+#include <unistd.h>
 
 using namespace std::placeholders;
 
+// Nexus patch 0006 (the real M5 SIGSEGV fix): the HTTP completion callback runs Minecraft's own
+// libHttpClient + the DRM mod's OnRequestCompleted, which recurse deep through a C++
+// promise/continuation graph. std::thread on macOS inherits the pthread DEFAULT stack of only
+// 512 KB — but that code is written for Android threads (>= 1 MB). On our undersized stack the
+// (bounded) chain overshoots the guard page; the next std::mutex::lock then reads a garbage
+// thread_mutex_t and faults in shim::pthread_mutex_lock. Run the worker on a pthread with a large
+// stack so the chain fits.
+static constexpr size_t NEXUS_HTTP_WORKER_STACK = 64u * 1024u * 1024u;  // 64 MB (lazily paged)
+
+static void nexus_spawn_http_worker(std::function<void()> fn) {
+    auto *heapFn = new std::function<void()>(std::move(fn));
+    pthread_attr_t attr;
+    if (pthread_attr_init(&attr) == 0) {
+        pthread_attr_setstacksize(&attr, NEXUS_HTTP_WORKER_STACK);
+        pthread_attr_setdetachstate(&attr, PTHREAD_CREATE_DETACHED);
+        pthread_t tid;
+        int rc = pthread_create(&tid, &attr, [](void *arg) -> void * {
+            auto *f = static_cast<std::function<void()> *>(arg);
+            (*f)();
+            delete f;
+            return nullptr;
+        }, heapFn);
+        pthread_attr_destroy(&attr);
+        if (rc == 0) return;
+    }
+    std::thread([heapFn]() { (*heapFn)(); delete heapFn; }).detach();  // fallback
+}
+
 bool slist_contains(struct curl_slist *list, const char *str) {
     struct curl_slist *current = list;
     while (current != nullptr) {
@@ -17,8 +48,20 @@ bool slist_contains(struct curl_slist *list, const char *str) {
     return false;
 }
 
+// Nexus: our statically-linked curl uses a custom OpenSSL whose baked-in OPENSSLDIR has no CA
+// bundle, so EVERY https request failed TLS verification (the game's profile/store/Xbox screens
+// spun forever, and pre-fix the failed-callback path aborted the game). Point curl at macOS's
+// system CA bundle, which every macOS ships at /etc/ssl/cert.pem.
+void nexus_curl_setup_cainfo(void *curl) {
+    static const bool has_system_pem = access("/etc/ssl/cert.pem", R_OK) == 0;
+    if(has_system_pem) {
+        curl_easy_setopt(curl, CURLOPT_CAINFO, "/etc/ssl/cert.pem");
+    }
+}
+
 HttpClientRequest::HttpClientRequest() {
     curl = curl_easy_init();
+    nexus_curl_setup_cainfo(curl);
     curl_easy_setopt(curl, CURLOPT_WRITEDATA, this);
     // Fallback if neither setHttpMethodAndBody or setHttpMethodAndBody2 has been called
     curl_easy_setopt(curl, CURLOPT_WRITEFUNCTION, HttpClientRequest::write_callback_wrapper_old);
@@ -138,12 +181,35 @@ void HttpClientRequest::setHttpHeader(std::shared_ptr<FakeJni::JString> name, st
     curl_easy_setopt(curl, CURLOPT_HTTPHEADER, header);
 }
 
+void HttpClientRequest::invokeRequestFailed(FakeJni::LocalFrame &frame, FakeJni::JLong sourceCall, bool isNoNetwork) {
+    try {
+        auto method4 = getClass().getMethod("(JLjava/lang/String;Ljava/lang/String;Ljava/lang/String;Z)V", "OnRequestFailed");
+        if(method4) {
+            method4->invoke(frame.getJniEnv(), this, sourceCall, frame.getJniEnv().createLocalReference(std::make_shared<FakeJni::JString>("Error")),
+                            frame.getJniEnv().createLocalReference(std::make_shared<FakeJni::JString>("")),
+                            frame.getJniEnv().createLocalReference(std::make_shared<FakeJni::JString>("")),
+                            isNoNetwork);
+            return;
+        }
+        auto method2 = getClass().getMethod("(JLjava/lang/String;)V", "OnRequestFailed");
+        if(method2) {
+            method2->invoke(frame.getJniEnv(), this, sourceCall, frame.getJniEnv().createLocalReference(std::make_shared<FakeJni::JString>("Error")));
+            return;
+        }
+        Log::error("HttpClient", "No OnRequestFailed native registered - dropping failure callback");
+    } catch(const std::exception &e) {
+        Log::error("HttpClient", "OnRequestFailed callback threw: %s", e.what());
+    } catch(...) {
+        Log::error("HttpClient", "OnRequestFailed callback threw an unknown exception");
+    }
+}
+
 void HttpClientRequest::doRequestAsync(FakeJni::JLong sourceCall) {
     call_handle = sourceCall;
     auto me = this->weak_from_this();
     FakeJni::LocalFrame frame;
     auto&& jvm = &frame.getJniEnv().getVM();
-    std::thread([=]() {
+    nexus_spawn_http_worker([=]() {
         FakeJni::JniEnvContext ctx(*jvm);
         try {
             auto anotherme = me.lock();
@@ -165,19 +231,13 @@ void HttpClientRequest::doRequestAsync(FakeJni::JLong sourceCall) {
                 Log::trace("HttpClient", "Response: code: %ld", response_code);
 #endif
                 auto method = getClass().getMethod("(JLcom/xbox/httpclient/HttpClientResponse;)V", "OnRequestCompleted");
-                method->invoke(frame.getJniEnv(), this, sourceCall, frame.getJniEnv().createLocalReference(std::make_shared<HttpClientResponse>(sourceCall, response_code, response, headers)));
-            } else {
-                // Detect if https://github.com/microsoft/libHttpClient/commit/bea2069547e6d480342476cf328b651584e2ada5 is compiled into the binary
-                if(NetworkObserver::getDescriptor()->getMethod("(Ljava/lang/String;)V", "Log")) {
-                    auto method = getClass().getMethod("(JLjava/lang/String;Ljava/lang/String;Ljava/lang/String;Z)V", "OnRequestFailed");
-                    method->invoke(frame.getJniEnv(), this, sourceCall, frame.getJniEnv().createLocalReference(std::make_shared<FakeJni::JString>("Error")),
-                                   frame.getJniEnv().createLocalReference(std::make_shared<FakeJni::JString>("")),
-                                   frame.getJniEnv().createLocalReference(std::make_shared<FakeJni::JString>("")),
-                                   ret == CURLE_COULDNT_RESOLVE_PROXY || ret == CURLE_COULDNT_RESOLVE_HOST || ret == CURLE_COULDNT_CONNECT);
+                if(method) {
+                    method->invoke(frame.getJniEnv(), this, sourceCall, frame.getJniEnv().createLocalReference(std::make_shared<HttpClientResponse>(sourceCall, response_code, response, headers)));
                 } else {
-                    auto method = getClass().getMethod("(JLjava/lang/String;)V", "OnRequestFailed");
-                    method->invoke(frame.getJniEnv(), this, sourceCall, frame.getJniEnv().createLocalReference(std::make_shared<FakeJni::JString>("Error")));
+                    Log::error("HttpClient", "No OnRequestCompleted(J,HttpClientResponse) native registered - dropping response");
                 }
+            } else {
+                invokeRequestFailed(frame, sourceCall, ret == CURLE_COULDNT_RESOLVE_PROXY || ret == CURLE_COULDNT_RESOLVE_HOST || ret == CURLE_COULDNT_CONNECT);
             }
         } catch(...) {
             if(me.expired()) {
@@ -185,18 +245,9 @@ void HttpClientRequest::doRequestAsync(FakeJni::JLong sourceCall) {
                 return;
             }
             FakeJni::LocalFrame frame(*jvm);
-            if(NetworkObserver::getDescriptor()->getMethod("(Ljava/lang/String;)V", "Log")) {
-                auto method = getClass().getMethod("(JLjava/lang/String;Ljava/lang/String;Ljava/lang/String;Z)V", "OnRequestFailed");
-                method->invoke(frame.getJniEnv(), this, sourceCall, frame.getJniEnv().createLocalReference(std::make_shared<FakeJni::JString>("Error")),
-                               frame.getJniEnv().createLocalReference(std::make_shared<FakeJni::JString>("")),
-                               frame.getJniEnv().createLocalReference(std::make_shared<FakeJni::JString>("")),
-                               false);
-            } else {
-                auto method = getClass().getMethod("(JLjava/lang/String;)V", "OnRequestFailed");
-                method->invoke(frame.getJniEnv(), this, sourceCall, frame.getJniEnv().createLocalReference(std::make_shared<FakeJni::JString>("Error")));
-            }
+            invokeRequestFailed(frame, sourceCall, false);
         }
-    }).detach();
+    });
 }
 
 FakeJni::JInt HttpClientResponse::getNumHeaders() {
diff --git a/src/jni/lib_http_client.h b/src/jni/lib_http_client.h
index d77583e..58742c2 100644
--- a/src/jni/lib_http_client.h
+++ b/src/jni/lib_http_client.h
@@ -61,6 +61,12 @@ private:
     std::string method;
     FakeJni::JLong call_handle;
 
+    // Report a failed request to whichever OnRequestFailed overload the game actually
+    // registered. Never throws: newer Bedrock (1.26.20.4+) registers ONLY the 4-arg variant
+    // and no NetworkObserver.Log, so signature guessing + an unguarded invoke aborted the
+    // whole game (uncaught std::runtime_error("No such Method!") from a worker thread).
+    void invokeRequestFailed(FakeJni::LocalFrame &frame, FakeJni::JLong sourceCall, bool isNoNetwork);
+
     size_t write_callback_old(char *ptr, size_t size, size_t nmemb);
     size_t write_callback(char *ptr, size_t size, size_t nmemb);
     size_t header_callback(char *buffer, size_t size, size_t nitems);
diff --git a/src/jni/lib_http_client_websocket.cpp b/src/jni/lib_http_client_websocket.cpp
index 52e7f14..7bc315f 100644
--- a/src/jni/lib_http_client_websocket.cpp
+++ b/src/jni/lib_http_client_websocket.cpp
@@ -46,6 +46,8 @@ HttpClientWebSocket::HttpClientWebSocket(FakeJni::JLong owner) {
 
 #ifdef ENABLE_WEBSOCKETS
     curl = curl_easy_init();
+    extern void nexus_curl_setup_cainfo(void *curl); // system CA bundle (see lib_http_client.cpp)
+    nexus_curl_setup_cainfo(curl);
     curl_easy_setopt(curl, CURLOPT_CONNECT_ONLY, 2L);
 #endif
 
diff --git a/src/main.cpp b/src/main.cpp
index 35cd788..91489b4 100644
--- a/src/main.cpp
+++ b/src/main.cpp
@@ -248,7 +248,10 @@ int main(int argc, char* argv[]) {
     readOptions();
 
 #ifdef __APPLE__
-    if(MinecraftVersion::isAtLeast(1, 26, 10, 0)) {
+    // Nexus patch 0003b: wire up the mvk-angle EGL library for ALL versions, not just 1.26.10+.
+    // Older versions (e.g. 1.21.x) also fall back to the ES2/EGL path when desktop-GL fails, and
+    // without elg_lib set GLFW throws "EGL: Library not found" and aborts at window creation.
+    if(FileUtil::exists(PathHelper::getAppDir() + "/../Frameworks/mvk-angle/libEGL.dylib")) {
         std::string appdir = PathHelper::getAppDir();
         std::string libEGL = appdir + "/../Frameworks/mvk-angle/libEGL.dylib";
         std::string MoltenVK_icd = appdir + "/../Frameworks/mvk-angle/MoltenVK_icd.json";
diff --git a/src/settings.cpp b/src/settings.cpp
index 576948c..61e3c7a 100644
--- a/src/settings.cpp
+++ b/src/settings.cpp
@@ -3,6 +3,7 @@
 #include <properties/property.h>
 #include <mcpelauncher/path_helper.h>
 #include <fstream>
+#include <log.h>
 
 std::optional<bool> Settings::enable_imgui = {};
 std::atomic<int> Settings::menubarsize = 0;
@@ -20,6 +21,37 @@ int Settings::enable_keystroke_hud;
 float Settings::keystroke_hud_x;
 float Settings::keystroke_hud_y;
 
+int Settings::nexus_fps;
+int Settings::nexus_fps_pos;
+float Settings::nexus_fps_scale;
+int Settings::nexus_cps;
+int Settings::nexus_cps_pos;
+float Settings::nexus_cps_scale;
+int Settings::nexus_keystrokes;
+int Settings::nexus_keystrokes_pos;
+float Settings::nexus_keystrokes_scale;
+int Settings::nexus_session_timer;
+int Settings::nexus_session_timer_pos;
+float Settings::nexus_session_timer_scale;
+int Settings::nexus_real_clock;
+int Settings::nexus_real_clock_pos;
+float Settings::nexus_real_clock_scale;
+int Settings::nexus_watermark;
+int Settings::nexus_watermark_pos;
+float Settings::nexus_watermark_scale;
+int Settings::nexus_crosshair;
+float Settings::nexus_crosshair_scale;
+int Settings::nexus_fps_graph;
+int Settings::nexus_fps_graph_pos;
+float Settings::nexus_fps_graph_scale;
+int Settings::nexus_discord_button;
+int Settings::nexus_toggle_sprint;
+float Settings::nexus_hud_opacity;
+int Settings::nexus_hud_accent;
+int Settings::nexus_hud_in_menus;
+float Settings::nexus_menu_scale;
+std::string Settings::nexus_menu_key;
+
 std::string Settings::videoMode;
 float Settings::scale;
 std::string Settings::menubarFocusKey;
@@ -53,6 +85,40 @@ static properties::property<int> enable_keystroke_hud(settings, "enable_keystrok
 static properties::property<float> keystroke_hud_x(settings, "keystroke_hud_x", /* default if not defined*/ 0);
 static properties::property<float> keystroke_hud_y(settings, "keystroke_hud_y", /* default if not defined*/ 0);
 
+// Nexus mods. Corner presets: 0 top left, 1 top right, 2 bottom left, 3 bottom right.
+static properties::property<bool> nexus_fps(settings, "nexus_fps", false);
+static properties::property<int> nexus_fps_pos(settings, "nexus_fps_pos", 0);
+static properties::property<float> nexus_fps_scale(settings, "nexus_fps_scale", 1.0f);
+static properties::property<bool> nexus_cps(settings, "nexus_cps", false);
+static properties::property<int> nexus_cps_pos(settings, "nexus_cps_pos", 0);
+static properties::property<float> nexus_cps_scale(settings, "nexus_cps_scale", 1.0f);
+static properties::property<bool> nexus_keystrokes(settings, "nexus_keystrokes", false);
+static properties::property<int> nexus_keystrokes_pos(settings, "nexus_keystrokes_pos", 2);
+static properties::property<float> nexus_keystrokes_scale(settings, "nexus_keystrokes_scale", 1.0f);
+static properties::property<bool> nexus_session_timer(settings, "nexus_session_timer", false);
+static properties::property<int> nexus_session_timer_pos(settings, "nexus_session_timer_pos", 0);
+static properties::property<float> nexus_session_timer_scale(settings, "nexus_session_timer_scale", 1.0f);
+static properties::property<bool> nexus_real_clock(settings, "nexus_real_clock", false);
+static properties::property<int> nexus_real_clock_pos(settings, "nexus_real_clock_pos", 1);
+static properties::property<float> nexus_real_clock_scale(settings, "nexus_real_clock_scale", 1.0f);
+static properties::property<bool> nexus_watermark(settings, "nexus_watermark", false);
+static properties::property<int> nexus_watermark_pos(settings, "nexus_watermark_pos", 0);
+static properties::property<float> nexus_watermark_scale(settings, "nexus_watermark_scale", 1.0f);
+static properties::property<bool> nexus_crosshair(settings, "nexus_crosshair", false);
+static properties::property<float> nexus_crosshair_scale(settings, "nexus_crosshair_scale", 1.0f);
+static properties::property<bool> nexus_fps_graph(settings, "nexus_fps_graph", false);
+static properties::property<int> nexus_fps_graph_pos(settings, "nexus_fps_graph_pos", 0);
+static properties::property<float> nexus_fps_graph_scale(settings, "nexus_fps_graph_scale", 1.0f);
+static properties::property<bool> nexus_discord_button(settings, "nexus_discord_button", true);
+static properties::property<bool> nexus_toggle_sprint(settings, "nexus_toggle_sprint", false);
+static properties::property<float> nexus_hud_opacity(settings, "nexus_hud_opacity", 0.55f);
+static properties::property<int> nexus_hud_accent(settings, "nexus_hud_accent", 0);
+static properties::property<bool> nexus_hud_in_menus(settings, "nexus_hud_in_menus", false);
+// Menu scale is new in nexus10. It enlarges the mod menu only (never the HUD),
+// so the menu reads well at 1440p and on small laptop screens. Default 1.3x.
+static properties::property<float> nexus_menu_scale(settings, "nexus_menu_scale", 1.3f);
+static properties::property<std::string> nexus_menu_key(settings, "nexus_menu_key", "RightShift");
+
 static properties::property<std::string> videoMode(settings, "videoMode", "");
 static properties::property<float> scale(settings, "scale", 1);
 static properties::property<std::string> menubarFocusKey(settings, "menubarFocusKey", "");
@@ -66,7 +132,14 @@ std::string Settings::getPath() {
 void Settings::load() {
     std::ifstream propertiesFile(getPath());
     if(propertiesFile) {
-        settings.load(propertiesFile);
+        // A value the parser cannot convert throws, and an escaped throw here
+        // takes the game down before it starts. Settings are not worth that:
+        // keep the defaults for whatever did not parse and carry on.
+        try {
+            settings.load(propertiesFile);
+        } catch(std::exception const& e) {
+            Log::warn("Settings", "%s could not be fully parsed: %s", getPath().c_str(), e.what());
+        }
     }
     if(::enable_imgui.get() != "auto") {
         properties::property_list dummy;
@@ -87,6 +160,37 @@ void Settings::load() {
     Settings::keystroke_hud_x = ::keystroke_hud_x.get();
     Settings::keystroke_hud_y = ::keystroke_hud_y.get();
 
+    Settings::nexus_fps = ::nexus_fps.get() ? 1 : 0;
+    Settings::nexus_fps_pos = ::nexus_fps_pos.get();
+    Settings::nexus_fps_scale = ::nexus_fps_scale.get();
+    Settings::nexus_cps = ::nexus_cps.get() ? 1 : 0;
+    Settings::nexus_cps_pos = ::nexus_cps_pos.get();
+    Settings::nexus_cps_scale = ::nexus_cps_scale.get();
+    Settings::nexus_keystrokes = ::nexus_keystrokes.get() ? 1 : 0;
+    Settings::nexus_keystrokes_pos = ::nexus_keystrokes_pos.get();
+    Settings::nexus_keystrokes_scale = ::nexus_keystrokes_scale.get();
+    Settings::nexus_session_timer = ::nexus_session_timer.get() ? 1 : 0;
+    Settings::nexus_session_timer_pos = ::nexus_session_timer_pos.get();
+    Settings::nexus_session_timer_scale = ::nexus_session_timer_scale.get();
+    Settings::nexus_real_clock = ::nexus_real_clock.get() ? 1 : 0;
+    Settings::nexus_real_clock_pos = ::nexus_real_clock_pos.get();
+    Settings::nexus_real_clock_scale = ::nexus_real_clock_scale.get();
+    Settings::nexus_watermark = ::nexus_watermark.get() ? 1 : 0;
+    Settings::nexus_watermark_pos = ::nexus_watermark_pos.get();
+    Settings::nexus_watermark_scale = ::nexus_watermark_scale.get();
+    Settings::nexus_crosshair = ::nexus_crosshair.get() ? 1 : 0;
+    Settings::nexus_crosshair_scale = ::nexus_crosshair_scale.get();
+    Settings::nexus_fps_graph = ::nexus_fps_graph.get() ? 1 : 0;
+    Settings::nexus_fps_graph_pos = ::nexus_fps_graph_pos.get();
+    Settings::nexus_fps_graph_scale = ::nexus_fps_graph_scale.get();
+    Settings::nexus_discord_button = ::nexus_discord_button.get() ? 1 : 0;
+    Settings::nexus_toggle_sprint = ::nexus_toggle_sprint.get() ? 1 : 0;
+    Settings::nexus_hud_opacity = ::nexus_hud_opacity.get();
+    Settings::nexus_hud_accent = ::nexus_hud_accent.get();
+    Settings::nexus_hud_in_menus = ::nexus_hud_in_menus.get() ? 1 : 0;
+    Settings::nexus_menu_scale = ::nexus_menu_scale.get();
+    Settings::nexus_menu_key = ::nexus_menu_key.get();
+
     Settings::videoMode = ::videoMode.get();
     Settings::scale = ::scale.get();
     Settings::menubarFocusKey = ::menubarFocusKey.get();
@@ -113,6 +217,37 @@ void Settings::save() {
     ::keystroke_hud_x.set(Settings::keystroke_hud_x);
     ::keystroke_hud_y.set(Settings::keystroke_hud_y);
 
+    ::nexus_fps.set(Settings::nexus_fps != 0);
+    ::nexus_fps_pos.set(Settings::nexus_fps_pos);
+    ::nexus_fps_scale.set(Settings::nexus_fps_scale);
+    ::nexus_cps.set(Settings::nexus_cps != 0);
+    ::nexus_cps_pos.set(Settings::nexus_cps_pos);
+    ::nexus_cps_scale.set(Settings::nexus_cps_scale);
+    ::nexus_keystrokes.set(Settings::nexus_keystrokes != 0);
+    ::nexus_keystrokes_pos.set(Settings::nexus_keystrokes_pos);
+    ::nexus_keystrokes_scale.set(Settings::nexus_keystrokes_scale);
+    ::nexus_session_timer.set(Settings::nexus_session_timer != 0);
+    ::nexus_session_timer_pos.set(Settings::nexus_session_timer_pos);
+    ::nexus_session_timer_scale.set(Settings::nexus_session_timer_scale);
+    ::nexus_real_clock.set(Settings::nexus_real_clock != 0);
+    ::nexus_real_clock_pos.set(Settings::nexus_real_clock_pos);
+    ::nexus_real_clock_scale.set(Settings::nexus_real_clock_scale);
+    ::nexus_watermark.set(Settings::nexus_watermark != 0);
+    ::nexus_watermark_pos.set(Settings::nexus_watermark_pos);
+    ::nexus_watermark_scale.set(Settings::nexus_watermark_scale);
+    ::nexus_crosshair.set(Settings::nexus_crosshair != 0);
+    ::nexus_crosshair_scale.set(Settings::nexus_crosshair_scale);
+    ::nexus_fps_graph.set(Settings::nexus_fps_graph != 0);
+    ::nexus_fps_graph_pos.set(Settings::nexus_fps_graph_pos);
+    ::nexus_fps_graph_scale.set(Settings::nexus_fps_graph_scale);
+    ::nexus_discord_button.set(Settings::nexus_discord_button != 0);
+    ::nexus_toggle_sprint.set(Settings::nexus_toggle_sprint != 0);
+    ::nexus_hud_opacity.set(Settings::nexus_hud_opacity);
+    ::nexus_hud_accent.set(Settings::nexus_hud_accent);
+    ::nexus_hud_in_menus.set(Settings::nexus_hud_in_menus != 0);
+    ::nexus_menu_scale.set(Settings::nexus_menu_scale);
+    ::nexus_menu_key.set(Settings::nexus_menu_key);
+
     ::videoMode.set(Settings::videoMode);
     ::scale.set(Settings::scale);
     ::menubarFocusKey.set(Settings::menubarFocusKey);
diff --git a/src/settings.h b/src/settings.h
index b377de8..589afed 100644
--- a/src/settings.h
+++ b/src/settings.h
@@ -20,6 +20,50 @@ struct Settings {
     static float keystroke_hud_x;
     static float keystroke_hud_y;
 
+    // ---- Nexus mods (nexus9) --------------------------------------------------
+    // Key names below match the ones nexus8 shipped, so a player's existing
+    // mcpelauncher-client-settings.txt carries over untouched. The *_scale,
+    // opacity, accent and menu-key entries are new in nexus9.
+    static int nexus_fps;
+    static int nexus_fps_pos;
+    static float nexus_fps_scale;
+
+    static int nexus_cps;
+    static int nexus_cps_pos;
+    static float nexus_cps_scale;
+
+    static int nexus_keystrokes;
+    static int nexus_keystrokes_pos;
+    static float nexus_keystrokes_scale;
+
+    static int nexus_session_timer;
+    static int nexus_session_timer_pos;
+    static float nexus_session_timer_scale;
+
+    static int nexus_real_clock;
+    static int nexus_real_clock_pos;
+    static float nexus_real_clock_scale;
+
+    static int nexus_watermark;
+    static int nexus_watermark_pos;
+    static float nexus_watermark_scale;
+
+    static int nexus_crosshair;
+    static float nexus_crosshair_scale;
+
+    static int nexus_fps_graph;
+    static int nexus_fps_graph_pos;
+    static float nexus_fps_graph_scale;
+
+    static int nexus_discord_button;
+    static int nexus_toggle_sprint;
+
+    static float nexus_hud_opacity;
+    static int nexus_hud_accent;
+    static int nexus_hud_in_menus;
+    static float nexus_menu_scale;
+    static std::string nexus_menu_key;
+
     static std::string videoMode;
     static float scale;
     static std::string menubarFocusKey;
diff --git a/src/window_callbacks.cpp b/src/window_callbacks.cpp
index 2838736..c3a851a 100644
--- a/src/window_callbacks.cpp
+++ b/src/window_callbacks.cpp
@@ -11,6 +11,7 @@
 #include "util.h"
 #ifdef USE_IMGUI
 #include "imgui_ui.h"
+#include "nexus_mods.h"
 #endif
 
 WindowCallbacks::WindowCallbacks(GameWindow& window, JniSupport& jniSupport, FakeInputQueue& inputQueue) : window(window), jniSupport(jniSupport), inputQueue(inputQueue) {
@@ -424,6 +425,16 @@ void WindowCallbacks::onKeyboard(KeyCode key, KeyAction action, int mods) {
         if(sharedInputState.wantCaptureKeyboard.load() || sharedInputState.wantTextInput.load()) {
             return;
         }
+
+        // Toggle Sprint rewrites or drops the sprint key before the game sees
+        // it, so one tap holds sprint until the next tap.
+        {
+            int filtered = NexusMods::filterSprintKey((int)key, (int)action);
+            if(filtered < 0) {
+                return;
+            }
+            action = (KeyAction)filtered;
+        }
 #endif
 // return onKeyboard((KeyCode) 4, KeyAction::PRESS);
 // key = (KeyCode) 0x21;
